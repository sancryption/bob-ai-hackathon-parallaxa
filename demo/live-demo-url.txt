NOT DEPLOYED — run locally using docs/setup-guide.md

The application runs on localhost only (no cloud deployment).
See docs/setup-guide.md for exact setup steps (approximately 5 minutes on a clean machine).

  Frontend: http://localhost:5173
  Backend API + Swagger UI: http://localhost:8000/api/docs
  Health check: http://localhost:8000/api/health
